export interface OrbitParams {
  radius: number;
  speed: number; // revolutions per second
}

export class OrbitEngine {
  constructor() {
    // TODO: setup orbit calculations
  }

  update(_deltaTime: number): void {
    void _deltaTime;
    // TODO: update orbit positions
  }
}
